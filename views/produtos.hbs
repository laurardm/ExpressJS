<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>{{titulo}}</title>
    <style>
        body {
            background-color: #f8f9fa;
        }

        .card-title strong {
            color: #0d6efd;
        }

        img {
            max-width: 100%;
            height: auto;
            margin-top: 0.5rem;
        }
    </style>
</head>

<body>
    <div class="container mt-4">
        <h1 class="display-5 mb-4">{{titulo}}</h1>

        {{#if produtosParaView.length}}
        <div class="row">
            {{#each produtosParaView}}
            <div class="col-md-6 col-lg-4 mb-3">
                <div class="card h-100">
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">
                            <strong>{{nome}}</strong>
                            <img src="{{imagem}}" alt="Imagem do produto {{nome}}">
                            <small class="text-muted fw-normal">ID: {{id}}</small>
                        </h5>
                        <p class="card-text flex-grow-1">{{descricao}}</p>
                        <a class="btn btn-sm btn-outline-primary mt-auto" href="#">Ver Detalhes</a>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
        {{else}}
        <div class="alert alert-warning" role="alert">
            Nenhum produto para exibir no momento.
        </div>
        {{/if}}

        <hr class="my-4">
        <a class="btn btn-secondary" href="/">Voltar para a página inicial</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>